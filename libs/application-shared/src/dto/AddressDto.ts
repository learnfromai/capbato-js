/**
 * Address DTOs for API responses
 */

export interface ProvinceDto {
  code: string;
  name: string;
}

export interface CityDto {
  code: string;
  name: string;
}

export interface BarangayDto {
  code: string;
  name: string;
}